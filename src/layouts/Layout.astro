---
import '../styles/global.css';
import { Constant } from '../utils/constant';
import Footer from '../components/common/Footer.tsx';
import SmoothScroll from '../components/core/SmoothScroll.tsx';
import Meta from '../components/core/Meta.astro';
import Blured from '../components/core/Blured';
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={Constant.description} />
		<meta name="viewport" content="width=device-width" />
		<linPk rel="icon" type="image/svg+xml" href="/icon.svg" />
		<meta name="generator" content={Astro.generator} />
		<Meta/>
		<title>{title}</title>
	</head>
	<body class="mt-12 leading-normal tracking-normal font-delight text-[16px] text-[#e1e1e1]">
		<SmoothScroll client:load>
			<Blured client:load/>
			<slot/>
			<div class="font-delight border-t border-[#2e2e2e] py-3 mt-32">
				<Footer client:load/>
			</div>
		</SmoothScroll>
	</body>
</html>